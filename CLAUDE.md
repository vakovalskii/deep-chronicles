# Хроники Глубин — заметки для Claude

Браузерная MMORPG-песочница в духе классических корейских MMO (механика своя, названия свои — никаких имён и ассетов чужих игр).
three.js r170 + Vite. Простые формы + пиксельные текстуры, нарисованные кодом (`src/tex.js`). Прогресс — в localStorage, сервер отвечает за присутствие игроков и чат.

## Запуск и релиз
- `npm install` → `npm run dev` (WS-сервер :8790 + Vite http://localhost:5173). `?ws=ws://…` — свой адрес сервера (E2E поднимает его на 8791).
- `npm test` — юнит-тесты (`tests/unit.test.js`, node:test) → сборка → E2E в Chrome (`tests/e2e.mjs`, playwright, сам поднимает vite на 5199).
- `bash deploy/deploy.sh` — **сначала `npm test`**, потом rsync `dist/` на `$DEPLOY_HOST:/opt/realms/dist` (адрес сервера — в переменной окружения, в репозиторий не коммитим), `server/` → `/opt/realms/server` и рестарт systemd `realms-ws` (порт 8790). Прод: https://realms.neuraldeep.ru (nginx `deploy/nginx-realms.conf`: статика + `/ws`, certbot).
- Добавил механику — добавь шаг в E2E или проверку в юнит-тесты.

## Структура
- `src/data.js` — классы (атрибуты СИЛ/ЛОВ/ВЫН/ИНТ/МДР/ДУХ), умения, `SLOTS` (12 слотов куклы), предметы (грейды, вес `w`, `set`, `twoHand`, `robe`, `full`), `SETS`, мобы, магазин, опыт.
- `src/stats.js` — без DOM: `calcStats` (атрибуты, экипировка, заточка `enchValue`, комплекты, перегруз), `equipFromBag`/`unequipSlot`, `wearError`, `migrate` (старые сохранения). Покрыт юнит-тестами.
- `src/tex.js` — пиксельные текстуры (`TEX.brick()` и т.д., серые — цвет даёт материал), `worldUV` — UV по мировым координатам для слитой геометрии.
- `server/server.js` — WS: `hello/st/look/chat` → `welcome/snap/look/leave/online/chat`. Снапшоты 10 Гц, радиус видимости 220 м, каналы all/trade/near с кулдаунами.
- `src/world.js` — рельеф (`heightAt`), зоны (`zoneAt`), города, лес/пустошь, склеп, лабиринт катакомб (отдельная площадка x ≥ 2200, пол y = 0), препятствия-круги `obstacles`, спавны, `TELEPORTS`. Статичный мир сливается по цвету в несколько мешей.
- `src/models.js` — процедурные модели героя/мобов/NPC, `userData.anim(t, state)`.
- `src/main.js` — рендер, небо-купол, ввод (ЛКМ — идти/цель/атака, ПКМ — камера, колесо — зум, 1–3 умения, 4–5 зелья, Tab, F, I, C, M, Enter — чат), бой (уклонение/промах), ИИ мобов, NPC, HUD, инвентарь (кукла + сетка, перетаскивание, `sel`, `enchMode`), окно персонажа, сеть (`net`, `remotes`, `lookOf/applyLook`), чат в окне лога (вкладки, `+текст` — торговля), частицы (`emit(kind, pos, opts)`, системы spark/dot/plus/flake/dust), сохранение `l2w-save1`.
- Внешний вид героя передаётся другим игрокам только через `lookOf()` (цвета и виды материалов); добавил новый вид — добавь поле в `cleanLook` на сервере.
- Тестовый хук `window.__g` — в dev или с `?test`.

## Принципы
- Интерфейс и комментарии — на русском.
- Далёкие мобы (> 160 м) не симулируются и не рисуются; подписи — до 20 ближайших.
- Точки телепорта и возрождения не должны попадать в препятствия (проверяется тестом).

## Дальше
Аккаунты и сохранение на сервере (сейчас ник не защищён, прогресс локальный), PvP и группы, торговля между игроками, больше классов и зон, осады.
